let isLoggedIn: boolean = true;


let myRegex: RegExp = /foo/;

const names: string[] = [];
const namesB: Array<String> = [];

names.push('Hello');
namesB.push('World');

interface Person {
    first: string,
    last: string;
}
const myPerson: Person = {
    first: "Conner",
    last: "Aiken" 
};

const mappedIds: Record<number,string> = {
    100: "Affordable",
    150: "Pricey",
    200: "Extravagent"
};

